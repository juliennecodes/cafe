const { expect, it} = require("@jest/globals");

//test usage
//this component is used for storing cart state
//this component is used for getting cart information from server
//this component is used for displaying the right component depending on the browser path

//should updateCart be tested here or in menuItem?
//are individual components even supposed to be tested or is it just the app as a whole?
 it('stores cart state', ()=> {
   //given render app
   //how do you access state?
   //cart is defined
 });
