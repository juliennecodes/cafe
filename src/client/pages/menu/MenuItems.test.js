const { expect, it} = require("@jest/globals");

//test usage
//this component is used for displaying the image, name, price, and button
//this component is used for updating the quantity of an item

it('displays an image', () => {
  //given render menu item
  //query for div
  //when menu item is rendered
  //then div should have a child that is img
});
//repeat for name, price, and button

it('can post information to the server', () => {
  //given render menu item
  //given button
  //query for button
  //when button is clicked
  //then post request is made to the server
  //then cart should update that cartItems item has udpated its quantity
});

//test for accurate information? is that needed?
